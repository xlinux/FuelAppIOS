import SwiftUI

enum Theme {
    static let background = Color(red: 0.05, green: 0.12, blue: 0.24)
    static let card = background
    static let accent = Color(red: 0.35, green: 0.62, blue: 1.00)
    static let text = Color.white
    static let secondaryText = Color.white.opacity(0.7)
}
